spring:
    datasource:
        url: jdbc:mysql://localhost:3306/vendorproduct
        username: root
        password: ALFANETWORK
        driver-class-name: com.mysql.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: create-drop
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5Dialect

    jackson:
        serialization:
            FAIL_ON_EMPTY_BEANS: false